> *This was generated by AI during triage.*

# Add moqxprobe stream-owner sender topology

Status: open
Type: AFK
Category: performance

## Parent

`.scratch/transport-layer-foundation/issues/26-harden-transport-pressure-abstractions.md`

## Related

- `.scratch/transport-layer-foundation/issues/47-refactor-transport-conn-stream-ownership.md`
- `.scratch/transport-layer-foundation/issues/46-improve-moqx-client-stream-throughput.md`
- `.scratch/transport-layer-foundation/issues/45-improve-moqx-client-mixed-stream-control-pressure.md`
- `docs/adr/0008-functional-conn-stream-ownership.md`

## What to build

Adapt `moqxprobe` so pure object-stream pressure can use the stream-local
`MOQX.Transport.Conn.Stream.Sender` API introduced by #47.

The current benchmark topology is `context_owner`: one Flow/GenStage sender
owns all stream admission and feeds send-completion credit through the shared
connection context loop. That shape is useful as a baseline, but it can turn
independent QUIC streams into one BEAM-side completion-credit pump.

Add an opt-in `stream_owner` topology for unidirectional `stream_pressure`.
Each stream gets its own worker process and its own
`Conn.Stream.Sender`. The worker schedules sends up to its per-stream window,
waits for that sender's completion events, and reports the same per-stream
diagnostic shape currently emitted by the context-owner path. Keep the JSONL
schema stable; distinguish the topology with existing
`stream_sender_topology` metadata and diagnostics.

Do not migrate mixed pressure in this slice. Mixed has an additional control
stream, echo validation, and scheduler policy, so it should be refit after the
object-stream-only lane proves whether stream-local ownership changes the
performance picture.

## Acceptance criteria

- [x] `moqxprobe measure` accepts
      `--stream-sender-topology context_owner|stream_owner`.
- [x] `stream_owner` is supported for
      `--topology moqx-client-to-reference-server --workload stream_pressure
      --stream-direction unidirectional`.
- [x] Unsupported combinations fail early with a clear validation error rather
      than silently falling back.
- [x] Each stream-owner worker owns one `MOQX.Transport.Conn.Stream.Sender`,
      uses send completions as backend-credit demand, and does not require the
      parent connection event loop for send-only completion accounting.
- [x] Output remains strict-valid `transport-bench-v1`, with
      `profile.settings.stream_sender_topology` and
      `diagnostics.summary.stream_sender_topology` set to the actual topology.
- [x] Local tests cover the new topology against a fake transport whose
      completion events are delivered to the calling worker process.
- [x] #46 records that fresh remote object-stream evidence must compare
      `context_owner` and `stream_owner` from the same artifact.

## Blocked by

None. #47 is done locally and provides the transport primitive.

## Activity

- 2026-06-12: Opened this issue after #47 to keep the benchmark-client
  topology work separate from the transport API refactor. Initial scope is
  unidirectional object-stream pressure only.
- 2026-06-12: Implemented opt-in
  `--stream-sender-topology stream_owner` for unidirectional
  `moqx-client-to-reference-server` stream pressure. The new path spawns one
  monitored worker per stream; each worker owns a
  `MOQX.Transport.Conn.Stream.Sender`, schedules up to its stream send window,
  and drains sender-local completion credit before reporting the existing
  per-stream diagnostic shape.
- 2026-06-12: Added the remote suite test name
  `moqx_object_stream_owner`. A single probed suite can now run
  `reference_object_stream,moqx_object_stream,moqx_object_stream_owner` and
  label the reference-server stats for all three connections.
- 2026-06-12: Remote x86-control evidence from artifact `666db4c` shows the
  first stream-owner implementation is correct but not fast enough. Runs
  `...-140836`, `...-141113`, and `...-141242` all passed strict validation.
  MOQX `stream_owner` stayed stable around 49.2-49.5 Mbps with 32,000
  completions, zero pending completions, and clean server ingress. MOQX
  `context_owner` remained bimodal: about 49 Mbps in two runs, then
  159.4 Mbps in the third. The third fast context-owner run had far fewer
  blocking receive calls (643 versus about 2,050 in slow runs) and a shorter
  active send span (about 1.84s versus about 6.1s). `stream_owner` issued
  32,000 nonzero-timeout receive calls and did not perform ready-event drains,
  so it serialized completion feedback one credit at a time per stream.
- 2026-06-12: Fixed the local stream-owner loop so one blocking sender receive
  is followed by zero-time ready drains up to `stream_event_batch_size` before
  the worker refills that stream's send window. The focused regression now
  proves drain calls are observed for immediately queued completion batches.
- 2026-06-12: Rebuilt artifact `2721984` with the ready-drain fix and reran
  two x86-control suites: `...-142043` and `...-142211`. The fix changed the
  diagnostics as expected: stream-owner blocking receive calls dropped from
  32,000 to about 5.3k-5.4k, and ready-drain calls rose to 32,000. Throughput
  did not materially change: stream-owner stayed around 49.1-49.5 Mbps, with
  32,000 completions, zero pending completions, and clean MOQX server ingress.
  This rejects "one blocking receive per completion" as the only explanation
  for the stream-owner ceiling.

## Verification

- 2026-06-12: `mix test test/moqxprobe/measure_test.exs --trace` in
  `bench/moqxprobe`: 23 tests, 0 failures. The new fake-transport regression
  emitted a strict-valid `transport-bench-v1` record with
  `stream_sender_topology=stream_owner`, 4 accepted sends, 4 send completions,
  and 0 pending send completions.
- 2026-06-12: `mix format` in `bench/moqxprobe`.
- 2026-06-12: `mix test` in `bench/moqxprobe`: 70 tests, 0 failures.
- 2026-06-12: `mix credo --strict` in `bench/moqxprobe`: no issues.
- 2026-06-12: root `mix format`.
- 2026-06-12: root `mix test`: 178 passed, 18 excluded.
- 2026-06-12: root `mix credo --strict`: no issues.
- 2026-06-12: `bash bench/probed/scripts/quicprobe_stats_summary_test.sh`:
  passed, including the three-way object-stream stats summary regression.
- 2026-06-12: `mix format`, `mix test` (12 tests), and
  `mix credo --strict` in `bench/probed`: no issues.
- 2026-06-12: root `mix format`, root `mix test` (178 passed,
  18 excluded), and root `mix credo --strict`: no issues after adding the
  suite-driver test name.
- 2026-06-12: `mix test test/moqxprobe/measure_test.exs:1397` in
  `bench/moqxprobe`: passed after adding stream-owner ready-drain regression.
- 2026-06-12: `mix format`, `mix test` (70 tests), and
  `mix credo --strict` in `bench/moqxprobe`: no issues after the ready-drain
  fix.
- 2026-06-12: root `mix format`, root `mix test` (178 passed,
  18 excluded), and root `mix credo --strict`: no issues after the ready-drain
  fix.

## Follow-up

Next #46 work should inspect the remaining stream-owner/process-model cost and
quicer/MsQuic completion-release cadence. The first stream-owner topology is
correct and instrumented, but it is not a performance win yet.
